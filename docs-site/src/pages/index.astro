---
import DocsLayout from "../layouts/DocsLayout.astro";
import { Code } from "astro-expressive-code/components";
---

<DocsLayout title="Introduction" hidePrevNext>
  <h1>Identity</h1>
  <p>
    Authentication and user-management as a single, deployable container.
    Pull a pinned image, point it at an
    <a href="https://github.com/elloloop/tenant-shard-db">EntDB</a> instance,
    and you have email/password signup, passkeys, TOTP, OAuth, JWT issuance,
    sessions, and an admin user-management surface — all over Connect-RPC
    on HTTP/JSON.
  </p>

  <h2>What you get</h2>
  <ul>
    <li><strong>Email + password</strong> — signup, login, change, recovery-email reset, lockout</li>
    <li><strong>Passkeys (WebAuthn)</strong> — registration and login flows</li>
    <li><strong>TOTP (2FA)</strong> — setup, verify, recovery codes</li>
    <li><strong>OAuth login</strong> — Google, Microsoft (server consumes pre-verified tokens from frontend SDKs)</li>
    <li><strong>QR cross-device login</strong> — initiate on a new device, approve from a logged-in one</li>
    <li><strong>JWT issuance</strong> with key rotation, plus <code>/.well-known/jwks.json</code> for downstream services</li>
    <li><strong>Sessions</strong> — list, revoke, sign-out-everywhere</li>
    <li><strong>Admin user management</strong> — invite, deactivate, reactivate, reset password, set quota</li>
    <li><strong>Groups</strong> + membership edges (graph-backed)</li>
    <li><strong>Audit log</strong> of all auth events</li>
  </ul>

  <h2>How it deploys</h2>
  <p>
    One container, one image, one EntDB. Treat it like
    <a href="https://github.com/elloloop/tenant-shard-db">tenant-shard-db</a>:
    deploy it once per product, fully isolated user pool. The container
    listens on <code>:80</code> for Connect-RPC, <code>:9090</code> for
    Prometheus metrics.
  </p>

  <Code code={`docker run -p 80:80 -p 9090:9090 \\
  -e GATEWAY_ENTDB_ADDRESS=entdb:50051 \\
  -e GATEWAY_DEFAULT_TENANT_ID=my-product \\
  -e GATEWAY_PASSKEY_RP_ID=my-product.com \\
  -e GATEWAY_PASSKEY_ORIGIN=https://my-product.com \\
  -e GATEWAY_TOTP_ISSUER="My Product" \\
  ghcr.io/elloloop/identity:0.1.0`} lang="bash" title="docker run" />

  <h2>Storage layout</h2>
  <p>
    All persistent state lives in EntDB. Identity reserves the type IDs
    <code>1–99</code> in the EntDB schema; other services consuming the same
    EntDB instance must use type IDs <code>100+</code> to avoid collisions.
  </p>
  <table>
    <tr><th>type_id</th><th>Node</th></tr>
    <tr><td>1</td><td>User</td></tr>
    <tr><td>2</td><td>Group</td></tr>
    <tr><td>5</td><td>RefreshToken</td></tr>
    <tr><td>6</td><td>Passkey</td></tr>
    <tr><td>7</td><td>TOTPSecret</td></tr>
    <tr><td>8</td><td>AuditEvent</td></tr>
  </table>

  <h2>Where to next</h2>
  <ul>
    <li><a href="/identity/docs/quickstart">Quick Start</a> — run the container and issue your first token</li>
    <li><a href="/identity/docs/concepts/architecture">Architecture</a> — how the pieces fit together</li>
    <li><a href="/identity/docs/installation/configuration">Configuration</a> — every <code>GATEWAY_*</code> variable</li>
    <li><a href="/identity/docs/api-reference/grpc">gRPC Reference</a> — every RPC, every message</li>
  </ul>
</DocsLayout>
